<nav class="nav">
  <div>
    <ul>
    <li><%= link_to t('links.home'), root_path, rel: "follow" %></li>
    <li><%= link_to t('links.se_loger'), holiday_rentings_path, rel: "nofollow" %></li>
    <li><%= link_to t('links.alentours'), alentours_path, rel: "nofollow" %></li>
    <li><%= link_to t('links.contact'), contact_path, rel: "nofollow" %></li>
    <li><%= link_to "Poster une annonce",new_renting_path, class: "button create", rel: "nofollow" %></li>
    </ul>
  </div>
  <% if params[:controller] != "blog_posts" %>
    <%= render 'shared/lang_nav' %>
  <% else %>
  <% end %>
</nav>
